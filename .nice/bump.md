[2026-05-23 20:07] patch: Drop unused `rollup-plugin-peer-deps-external` devDependency — externalization is now handled by `nice-configuration/rollup`'s built-in peerDependencies reader. No build-output change.
[2026-05-23 09:00] major: Promote borderColor prop to token type. Consumer-facing type narrows from `string` to `BorderColorType` (re-exported as `ButtonBorderColorType`). Raw CSS color strings are no longer accepted — use a token variant (`"base"`, `"dark"`, `"darker"`). Internal style resolves via `getToken`. Closes action item 2 of manifest/.nice/reports/audit/token-bindings.md.
[2026-05-22 18:36] patch: Delete unused ButtonIcon component. The component was never exported from src/index.ts or package.exports.json — referenced only by its own test file. Removed the entire src/components/ButtonIcon/ folder (component, types, tests, index). No consumer impact; closes the legacy ButtonIcon entry from the token-bindings audit.
[2026-05-20 15:08] major: `mode` prop now wraps Button in `<Mode name={mode}>` instead of pinning the styled-component to that mode in isolation. getStatusToken signature simplified — third `mode` argument removed; status/state colors resolve via cascade. Internal Typography child still receives invertedMode (button text contrast preserved).
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Button's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).
