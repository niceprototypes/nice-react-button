[2026-06-09 02:35] patch: Update getStatusToken path order to [status, type, property, state] to match nice-styles' reordered status tokens. Rendered output unchanged.
[2026-06-12 16:37] patch: Color the icon with the link token to match the text when link is true, instead of leaving it pinned to lightest.
[2026-06-12 16:37] minor: Add a weight prop that sets the button label's font weight, forwarded to Typography.
[2026-06-19 19:44] patch: Migrate getToken-family calls to the options-object signature (nice-styles major). Internal refactor — rendered output unchanged; rebuild required for dependents.
[2026-06-21 21:06] patch: Drive border-width and border-color from button component tokens (getButtonToken) instead of the core tokens, so they can be overridden per-subtree via --np--button--border-*. Rendered output unchanged at defaults
[2026-06-27 16:05] patch: Migrate the getBreakpoint call to the new nice-styles +/- key grammar (getBreakpoint(`${BREAKPOINT_TABLET}+`)). Internal — rendered output unchanged.
[2026-07-01 18:27] major: Button status default flips — ButtonStatusType is now "base" | "primary" (was "primary" | "secondary") and status defaults to "base" (the old secondary/outline look). primary is unchanged but must now be passed explicitly to get the filled/emphasis look. Bare <Button> renders the base (formerly secondary) style; status="secondary" is removed — use the default (base) or status="primary".
[2026-07-03 11:35] minor: Button icon color now follows the button’s own color, replacing the hardcoded lightest/link. StyledButton sets --np--icon--color: currentColor so the icon tracks the button’s resolved color across status/state/theme.
[2026-07-10 16:03] minor: Add a padding prop (ButtonPaddingType = FlexSpacingType — a 1–4 value gap-token shorthand, e.g. "none base larger") following the nice-react-flex padding standard. The icon(s)+label now render inside an inner nice-react-flex row (ButtonContent) that carries the padding; when padding is set, StyledButton drops its default size-derived horizontal padding and switches height→min-height so non-zero vertical values grow the button. Omitted → existing size-derived horizontal padding and fixed height, so existing consumers render unchanged. Square (icon-only) buttons ignore padding. Requires nice-react-flex (already a peer dep).
[2026-07-10 16:19] patch: Route the nested icon color through the new --np--button--icon--color token layer instead of hardcoding currentColor. StyledButton now sets --np--icon--color: var(--np--button--icon--color) (default currentColor), so button icons can be recolored via --np--button--icon--color without touching the global --np--icon--color. Requires nice-styles with the --np--button--icon--color token. Rendered output unchanged at defaults.

[2026-07-10 17:16] major: Remove the status prop (ButtonStatusType "base"|"primary") and getStatusToken util; add a boolean filled prop (ButtonFilledType, @default false). filled=true: state color as background+border, label/icon in --np--color--inverse. filled=false (default, replaces the old base/outline look): transparent fill, state-colored border+label. Colors derive from getToken("color", state) — state name maps 1:1 to a --np--color variant — so no button status tokens are needed. Migrate status="primary" -> filled; status="base" -> default (drop the prop). Requires nice-styles without --np--button--status-*.

[2026-07-10 17:29] major: Rename the state prop to status (and ButtonStateType -> ButtonStatusType, ButtonTypes.State -> Status). Values are unchanged ("base" | "disabled" | "error" | "success" | "warning"); this is a pure identifier rename because state collides with React component-state convention. Migrate state="error" -> status="error", etc. Note: status previously named the base/primary emphasis prop (now removed in favor of filled), so status now denotes the semantic state.

[2026-07-11 10:00] major: Remove the deprecated `icon` prop. It was a back-compat alias mapping to `iconRight` (an explicit `iconRight` won). Use `iconLeft`/`iconRight` directly. Migrate `icon="x"` -> `iconRight="x"`.

[2026-07-11 10:20] minor: Route the nested icon size through the new --np--button--icon--size token layer, mirroring the --np--button--icon--color layer. The icon is pinned to its "base" size and StyledButton reassigns --np--icon--size to var(--np--button--icon--size[--$size]) at the button's own $size. The token defaults to the --np--font-size scale — the same scale the label reads via getToken("fontSize", $size) — so button icons match the label's font size at every size (rendered size unchanged at base; small/large buttons now get proportionally sized icons instead of a fixed base-size icon). Override --np--button--icon--size[--variant] to resize button icons without touching the global --np--icon--size. Requires nice-styles with the --np--button--icon--size token set.

[2026-07-11 10:40] major: Replace the boolean link/anchor prop with a polymorphic `as` prop (ButtonAsType, only "a" for now). `as="a"` renders an HTML anchor instead of <button> and forces an inlined, chrome-stripped layout — no fill/border/height/size-derived padding, link color, inline-flex, content width — so it flows in text like a link; href/target apply (rel="noopener noreferrer" auto-added for target="_blank"). Without `as`, Button renders a <button> unchanged. Types/namespace: added As/Href/Target. Migrate <Button link href=...> -> <Button as="a" href=...>.

[2026-07-11 11:40] patch: Internal — rename the ButtonContent flex's inline prop to inlined to track nice-react-flex's inline->inlined rename. Rendered output unchanged. Requires nice-react-flex with the inlined prop.
