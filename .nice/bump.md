[2026-05-20 15:08] major: `mode` prop now wraps Button in `<Mode name={mode}>` instead of pinning the styled-component to that mode in isolation. getStatusToken signature simplified — third `mode` argument removed; status/state colors resolve via cascade. Internal Typography child still receives invertedMode (button text contrast preserved).
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Button's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).
