import { createGlobalStyle } from "styled-components"

/**
 * Global styles for nice-react-button
 * Add your custom CSS variables here to style the button component
 */
const GlobalButtonStyles = createGlobalStyle`
  :root {
    /* Add your custom CSS variables here */
  }
`

export default GlobalButtonStyles